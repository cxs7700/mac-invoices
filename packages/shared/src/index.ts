// Shared Zod schemas and TypeScript types consumed by both @mac-invoices/web and
// @mac-invoices/api. Real invoice schemas (see PROJECT_PLAN.md §6) land in Phase 2;
// this placeholder exists so the workspace resolves and both apps can import it.

export const SHARED_PACKAGE_VERSION = '0.0.0'
